<!DOCTYPE html>
<html>
  <head>
    <%- include('./head'); %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-2"><%- include('./sidebar'); %></div>
        <div class="col-sm-10">
          <h1><%= title %></h1>
          <p>
            <strong>Do you really want to delete this book instance?</strong>
          </p>
          <div style="margin-left: 20px">
            <p><strong>ID:</strong><%= bookinstance._id %></p>
            <p><strong>Title:</strong><%= bookinstance.book.title %></p>
            <p><strong>Imprint:</strong><%= bookinstance.imprint %></p>
            <p>
              <strong>Status:</strong>
              <% if (bookinstance.status =='Available') { %>
              <span class="text-success"><%= bookinstance.status %></span>
              <% } else if (bookinstance.status=='Maintenance') { %>
              <span class="text-danger"><%= bookinstance.status %></span>
              <% } else { %>
              <span class="text-warning"><%= bookinstance.status %></span>
              <% } %>
            </p>
            <% if (bookinstance.status !='Available') { %>
            <span
              ><strong>Due back: </strong><%= bookinstance.due_back_formatted
              %></span
            >
            <% } %>
            <form method="post" action="">
              <div class="form-group">
                <input
                  class="id form-control"
                  type="hidden"
                  name="id"
                  required="true"
                  value="<%=bookinstance._id %>"
                />
              </div>
              <button class="btn btn-primary" type="submit">Delete</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
