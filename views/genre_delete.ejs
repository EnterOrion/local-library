<!DOCTYPE html>
<html>
  <head>
    <%- include('./head'); %>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row">
        <div class="col-sm-2"><%- include('./sidebar'); %></div>
        <div class="col-sm-10">
          <h1>Delete genre: <%= genre.name %></h1>
          <% if (genre_books.length !== 0) { %>
            <p>
              <strong
                >Delete the following books before attempting to delete this genre.
              </strong>
            </p>
            <div style="margin-left:20px;margin-top:20px">
          <h4>Books</h4>
          <dl>
              <% for (let i = 0; i <
            genre_books.length; i++) {%>
              <dt>
                <a href="<%= genre_books[i].url %>"><%= genre_books[i].title %></a>
                <dd><%= genre_books[i].summary %></dd>
              </dt>
  
              <%} %>
          </dl>
        </div>
        <%} else { %>
          <p>Do you really want to delete this genre?</p>
          <form method="post" action="">
            <div class="form-group">
                <input class="id form-control"
                type="hidden" name="id" required="true"
                value="<%=genre._id %>"
                >
                
                </div>
                <button class="btn btn-primary" type="submit">Delete</button>
            </form>
          <% } %>
      </div>
    </div>
  </body>
</html>
